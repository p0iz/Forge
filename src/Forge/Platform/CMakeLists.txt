# Platform submodule
#
# Contains event, input and window handling implementations for supported platforms

add_subdirectory(Event)
add_subdirectory(Input)
add_subdirectory(FileSystem)
add_subdirectory(Window)
